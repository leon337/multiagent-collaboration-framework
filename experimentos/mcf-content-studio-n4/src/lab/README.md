# Video Lab Shell

The Lab uses `@remotion/player` to preview a React video component with editable `inputProps`.

The shell is intentionally registry-agnostic. Integration provides `LabEntry[]`.

MVP controls:
- component selection;
- aspect switch;
- reduced-motion switch;
- prop editor;
- JSON spec preview.

No arbitrary code editor is included.
