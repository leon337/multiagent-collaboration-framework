name: Rede Social Free Pilot Format One

on:
  pull_request:
    paths:
      - ".github/workflows/rede-social-free-pilot-format-one.yml"
      - "apps/rede-social-agentes/ops/free-pilot-deploy.test.mjs"

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: apps/rede-social-agentes
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: implementation/rede-social-free-public-pilot

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24.18.0

      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.17.0 --activate

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Format targeted file
        run: pnpm exec prettier --write ops/free-pilot-deploy.test.mjs

      - name: Commit exact formatter output
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ops/free-pilot-deploy.test.mjs
          if ! git diff --cached --quiet; then
            git commit -m "chore(free-pilot): formatar teste operacional"
            git push origin HEAD:implementation/rede-social-free-public-pilot
          fi
