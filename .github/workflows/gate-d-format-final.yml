name: Gate D Formatting Final Apply

on:
  push:
    branches:
      - feat/mcf-runtime-006-gate-d-staging-deploy-adapter
    paths:
      - ".github/workflows/gate-d-format-final.yml"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24.18.0
      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.17.0 --activate
      - name: Install dependencies
        working-directory: apps/rede-social-agentes
        run: pnpm install --frozen-lockfile
      - name: Format remaining Gate D files
        working-directory: apps/rede-social-agentes
        run: |
          pnpm exec prettier --write \
            apps/server/src/mcf-runtime/github-staging-deploy.evidence.test.ts \
            apps/server/src/mcf-runtime/skill-executor.staging-deploy.test.ts \
            apps/server/src/mcf-runtime/skill-executor.ts
          pnpm exec prettier --check \
            apps/server/src/mcf-runtime/github-staging-deploy.evidence.test.ts \
            apps/server/src/mcf-runtime/skill-executor.staging-deploy.test.ts \
            apps/server/src/mcf-runtime/skill-executor.ts
      - name: Remove temporary workflow
        run: git rm .github/workflows/gate-d-format-final.yml
      - name: Enforce exact changed path set
        run: |
          expected=$(mktemp)
          actual=$(mktemp)
          cat >"$expected" <<'EOF'
          .github/workflows/gate-d-format-final.yml
          apps/rede-social-agentes/apps/server/src/mcf-runtime/github-staging-deploy.evidence.test.ts
          apps/rede-social-agentes/apps/server/src/mcf-runtime/skill-executor.staging-deploy.test.ts
          apps/rede-social-agentes/apps/server/src/mcf-runtime/skill-executor.ts
          EOF
          git diff HEAD --name-only | sort >"$actual"
          sort -o "$expected" "$expected"
          diff -u "$expected" "$actual"
          git diff HEAD --check
      - name: Commit deterministic formatting
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            apps/rede-social-agentes/apps/server/src/mcf-runtime/github-staging-deploy.evidence.test.ts \
            apps/rede-social-agentes/apps/server/src/mcf-runtime/skill-executor.staging-deploy.test.ts \
            apps/rede-social-agentes/apps/server/src/mcf-runtime/skill-executor.ts
          git commit -m 'style(gate-d): format remaining Gate D files'
          git push origin HEAD:feat/mcf-runtime-006-gate-d-staging-deploy-adapter
