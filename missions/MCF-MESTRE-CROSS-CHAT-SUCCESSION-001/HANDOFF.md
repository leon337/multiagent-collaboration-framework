# HANDOFF — MCF-MESTRE-CROSS-CHAT-SUCCESSION-001

## Successor instruction

You are the successor MESTRE for an active MCF mission. Do not assume this file is sufficient by itself.

Before any mutation:

1. read `ROADMAP.md`;
2. read `CHECKPOINT.yaml`;
3. read `EXPECTED_STATE.yaml`;
4. read `EVIDENCE.md`;
5. reconcile PR #175 and `main` against live GitHub;
6. produce your own `RECOVERED_STATE.yaml`;
7. compare it against `EXPECTED_STATE.yaml`;
8. declare mismatches and uncertainties;
9. do not merge until `SUCCESSION_EQUIVALENCE = PASS`.

## Human authority

LEANDRO remains final human authority.

Canonical gate phrase:

`HUMANO NO CONTROLE`

Semantics: stop new actions immediately, preserve state, checkpoint, enter HUMAN_GATE, and wait for explicit resumption.

## Current material mission

After succession equivalence passes, continue the qualified MCF v1.2.0 release mission:

- persist final LÉO/RENATO/EMILY opinions;
- verify `main` drift;
- merge exact PR #175 candidate only if still qualified;
- perform post-merge qualification;
- create tag `v1.2.0`;
- publish GitHub Release;
- verify tag → SHA → release;
- persist final receipt.

## Required honesty

Do not claim universal MissionRuntime pause/resume enforcement. The v1.2.0 candidate explicitly does not make that guarantee.

Do not inherit conclusions blindly from this handoff. Reconcile persistent sources and show evidence.
