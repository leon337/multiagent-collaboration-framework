# TEAM CONSENSUS — GUI / Window Control Finding — 2026-08-27

**Mission:** `MCF-MESTRE-CROSS-CHAT-SUCCESSION-001`  
**Authority:** LEANDRO  
**Coordinator:** MESTRE successor  
**Status:** `TEAM_CONSENSUS = PASS`  
**Scope:** classification and candidate next-release direction only

## Proposition under consensus

The team is deciding only whether the evidence supports the following non-normative formulation:

```text
CROSS_CHAT_SUCCESSION historical result = PASS
newly discovered GUI/window surface invariant coverage = GAP / NOT TESTED IN ORIGINAL RUN
future candidate scope = add explicit surface-preservation, window-identity and GUI regression criteria
```

This consensus does **not** by itself officialize a protocol rule, authorize `main` mutation, choose a version number, merge, tag or release.

## Evidence basis

### LÉO — Continuity and Internal Gates

Prior valid opinion: `APPROVE`, severity `MEDIUM-HIGH`.

LÉO stated that the newly discovered surface-isolation invariant was not actually tested, making the old experiment conceptually incomplete with respect to that invariant, while also stating that persistent recovery and the material release were not harmed.

### BEATRIZ — Agent Evaluation

Prior valid opinion: `ADOPT_WITH_CONDITIONS`, severity `MEDIUM`.

BEATRIZ explicitly rejected retrospective invalidation of `CROSS_CHAT_SUCCESSION = PASS` while requiring distinct surface identity, preserved predecessor, visual assertion, explicit predecessor-close action and regression tests.

### RENATO — Quality and Tests

Resumed independent model route: `oc/laguna-s-2.1-free`.

```text
VERDICT = ADOPT_WITH_CONDITIONS
SEVERITY = MEDIUM-HIGH
RETROSPECTIVE_CROSS_CHAT_PASS = MAINTAIN_WITH_GAP
```

RENATO's testing interpretation: preserve the historical PASS because it met the acceptance criteria then in force; record the new window/surface invariant as a previously uncovered regression dimension.

### AUGUSTO — Multiagent Observability

Resumed independent model route: `oc/mimo-v2.5-free`.

```text
SEVERITY = MEDIUM
RETROSPECTIVE_CROSS_CHAT_PASS = MAINTAIN_WITH_GAP
```

AUGUSTO separates logical/persistent succession from OS-window visual copresence and supports explicit trace fields for both dimensions.

### EMILY — Independent Audit

Independent one-shot route: `NVIDIA NIM / minimaxai/minimax-m3`.

```text
VERDICT = PASS_WITH_CONDITIONS
EVIDENCE_SUFFICIENCY = SUFFICIENT
FINDING_SEVERITY = MEDIUM
RETROSPECTIVE_CROSS_CHAT_PASS = MAINTAIN_WITH_GAP
LEO_BEATRIZ_RECONCILIATION = ACCEPT
RELEASE_GATE = BLOCK
```

Emily accepted the dimensional reconciliation and identified mandatory remediations before any future release containing this change.

## Reconciliation of LÉO × BEATRIZ

The original wording differed, but the propositions are not mutually exclusive:

1. **Historical validity:** BEATRIZ, RENATO, AUGUSTO and EMILY support that the original PASS satisfied the criteria actually in force and must not be erased retroactively.
2. **Coverage completeness:** LÉO's concern remains preserved: the original run did not prove predecessor-surface preservation or distinct successor-window identity because that invariant had not yet been defined/tested.
3. **Common classification:** `MAINTAIN_WITH_GAP` preserves both facts without rewriting history or pretending the new invariant was previously tested.

MESTRE therefore finds no remaining material disagreement on the classification or candidate direction.

## Consensus decision

```text
TEAM_CONSENSUS = PASS
CONSENSUS_CLASSIFICATION = MAINTAIN_WITH_GAP
FINDING_SEVERITY_RANGE = MEDIUM..MEDIUM-HIGH
NEXT_RELEASE_SCOPE_CANDIDATE = PASS
PROTOCOL_RULE_OFFICIAL = NO
```

The conditions are requirements on implementation/qualification, not unresolved disagreement about the evidence.

## Candidate remediations agreed by the panel

- explicit `OPEN_NEW_WINDOW` versus `OPEN_NEW_CHAT` distinction;
- predecessor surface preserved through `SUCCESSION_EQUIVALENCE` + explicit handoff;
- distinct successor logical session identity and OS-window/surface identity;
- two-window visual assertion;
- predecessor close/replacement as separate governed action;
- truthfulness boundary: `X11_SYNTHETIC_EVENT != DEVICE_LEVEL_INPUT_EVENT`;
- device-level input evidence when claiming keyboard shortcut execution;
- monitor-aware placement/tiling;
- observable/logged shortcut execution;
- simultaneous predecessor/successor copresence regression test;
- audit-trail note that the original run did not test this newly discovered invariant;
- independent revalidation for future gate-critical decisions involving this invariant.

## Governance boundary after consensus

```text
TEAM_CONSENSUS = PASS
HUMAN_GATE = READY_TO_OPEN_FOR_LEANDRO
PROTOCOL_MUTATION = NOT_YET_AUTHORIZED
MAIN_MUTATION = NONE
MERGE = NOT_AUTHORIZED
TAG = NOT_AUTHORIZED
RELEASE = NOT_AUTHORIZED
NEXT_VERSION = NOT_DECIDED
```

The next valid transition is a HUMAN_GATE addressed exclusively to LEANDRO. A human approval at this gate may authorize formalization/implementation of the candidate rules. Merge/tag/release remain separate evidence-dependent actions unless LEANDRO explicitly authorizes their conditions and the resulting candidate is qualified.
