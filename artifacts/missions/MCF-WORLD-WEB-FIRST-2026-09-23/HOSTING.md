# Hosting Direction (draft)

The hosted browser client is the primary runtime target. Electron remains an optional desktop shell.

Production deployment is not part of the current automatic boundary; this artifact only establishes topology and prerequisites.
