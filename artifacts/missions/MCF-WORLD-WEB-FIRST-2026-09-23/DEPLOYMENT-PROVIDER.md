# Deployment Provider

Decision: `DEFERRED_UNTIL_BUILD_VERIFIED`.

The web app should produce a portable static build first. Hosting-provider selection comes after artifact, caching, routing, HTTPS, rollback, and environment needs are verified. No production deployment is authorized by this mission artifact.
